import { InputText } from "./style"

export const Input = ({

    placeholder,
    fieldValue,
    onChangeText,
    keyType,
    maxLength,
    onEndEditing


}) => {
    return (
        <InputText />

    )
}